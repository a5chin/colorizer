name: Linting
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install formatter
      run: |
        python -m pip install --upgrade pip
        pip install black flake8 isort pydocstyle toml

    - name: Test with isort
      run: |
<<<<<<< HEAD
        isort --check colorize/

    - name: Test with black
      run: |
        black --check colorize/

    - name: Test with flake8
      run: |
        flake8 colorize/ --statistics --count --show-source
=======
        isort --check colorize/ examples/

    - name: Test with black
      run: |
        black --check colorize/ examples/

    - name: Test with flake8
      run: |
        flake8 colorize/ examples/ --statistics --count --show-source
>>>>>>> 4fc66d897e10c9c2619678a8dfd28828f6e383e2
